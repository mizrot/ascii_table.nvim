# ascii-table.nvim

Simple ASCII table manipulation for Neovim.

## Installation (lazy.nvim)

```lua
{
    "mizrot/ascii-table.nvim",
    config = function()
        require("ascii_table").setup()
    end,
}
```
